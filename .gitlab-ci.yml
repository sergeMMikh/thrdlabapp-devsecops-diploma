stages:
  - lint

lint:
  stage: lint
  image: python:3.10-slim

  before_script:
    - python --version
    - pip install --no-cache-dir -r requirements-lint.txt

  script:
    - python -m flake8 .

  rules:
    - if: "$CI_COMMIT_BRANCH"
