# Исправления по artifacts(1).zip

Исходный отчёт: pipeline `2864446165`, commit `03d8e4d`.
Проверка исправлений: 20 сентября 2026.

## Docker image и release gate

Исходный Trivy: 48 HIGH, 51 MEDIUM, 57 LOW, 1 UNKNOWN; pip-audit:
0 уязвимостей зависимостей приложения. Большинство HIGH относится к
системным пакетам Debian, для которых отчёт не указывает FixedVersion.
Остальные относятся к setuptools и вложенным библиотекам инструментов сборки.

Рабочий образ переведён на `python:3.12-alpine3.24`. Зависимости устанавливаются
из готовых wheels в отдельном build stage. В runtime отсутствуют pip,
setuptools и инструменты сборки; системные пакеты обновляются через apk.
Добавлен HEALTHCHECK. Сборка CI использует `--pull`, test-job — Python 3.12.
Порог блокировки HIGH/CRITICAL не изменён, исключения CVE не добавлялись.

Локальный Trivy 0.66.0 с обновлённой базой: 0 уязвимостей в новом образе.
Это результат конкретного сканирования, а не гарантия для будущих обновлений.

## Код

- `next` после удаления бронирования проверяется через
  `url_has_allowed_host_and_scheme`: разрешён текущий host, для HTTPS запрещено
  перенаправление на HTTP.
- `action` бронирования проходит ChoiceField и берётся из `cleaned_data`.
- Запросы `client.py` ограничены таймаутом 10 секунд.
- Для трёх CDN CSS добавлены SHA-384 integrity и crossorigin.
  Bootstrap проверен по [официальному примеру](https://getbootstrap.com/docs/5.2/getting-started/introduction/),
  все три хеша сверены с полученными файлами.
- Регистрация проверяет пароль с атрибутами пользователя до записи в БД.
  Клиент больше не может самостоятельно выставить `email_is_verified`;
  подтверждение по токену не активирует отключённый аккаунт.

## Разбор оставшихся сообщений

- Bandit: HIGH/MEDIUM — 0. LOW B101/B106 относятся к тестовым assert и
  фиктивным паролям. Проверки не отключены, тесты не удалены.
- Semgrep 1.136.0, `p/default` + `p/django`: 347 правил, 0 находок.
  Остаются 12 предупреждений PartialParsing для синтаксиса Django templates;
  результат не означает полного покрытия этих шаблонов.
- Точечные `nosemgrep: unvalidated-password` объясняют два ложных срабатывания:
  менеджер модели принимает доверенные вызовы, а старый неиспользуемый модуль
  `users/user_views.py` уже проверяет пароль в try/except/else.
  Такое поведение менеджера соответствует
  [документации Django](https://docs.djangoproject.com/en/5.2/topics/auth/passwords/).
  Активный API регистрации проверяет пароль явно и покрыт тестами.
- TruffleHog в исходном архиве: 0 verified, 2 unverified GitLab-находки в
  README старого commit `17964ffa533cdd0034f4732ae4e42e78266d8249`.
  Значения секретов здесь не публикуются. История не переписана; статус отзыва
  токенов нельзя установить из отчёта. Владельцу нужно проверить их отзыв в
  GitLab. Эти находки не объявляются исправленными.

## Проверки

- pytest: 67 passed, включая Selenium и регрессионные проверки безопасности.
- Django test runner: 5 passed.
- flake8: passed.
- Образ собирается; PostgreSQL migrations и collectstatic выполняются;
  Gunicorn запускается от пользователя django, главная страница отвечает 200.

Локальные подробные отчёты лежат в игнорируемой `.venv/`:
`trivy-fixed-image.json`, `bandit-fixed-report.json`, `semgrep-fixed-report.json`.
Повторный GitLab pipeline формирует независимые отчёты для нового commit.
